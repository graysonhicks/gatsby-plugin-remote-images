{
  "name": "gatsby-plugin-remote-images",
  "version": "1.0.0",
  "description": "Gatsby plugin download remote images from string fields on other nodes.",
  "author": "Grayson Hicks <graysonhicks@gmail.com>",
  "main": "gatsby-node.js",
  "files": [
    "gatsby-node.js",
    "README.md"
  ],
  "repository": "graysonhicks/gatsby-plugin-remote-images",
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "plugin",
    "images",
    "remote",
    "download"
  ],
  "license": "MIT",
  "peerDependencies": {
    "gatsby": ">2.0.0-alpha"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "babel-preset-gatsby-package": "^0.1.3",
    "cross-env": "^5.1.4"
  },
  "dependencies": {
    "gatsby-source-filesystem": "^2.0.11",
    "lodash": "^4.17.11"
  },
  "scripts": {
    "build": "babel src --out-dir . --ignore **/__tests__",
    "prepare": "cross-env NODE_ENV=production npm run build",
    "watch": "babel -w src --out-dir . --ignore **/__tests__"
  }
}

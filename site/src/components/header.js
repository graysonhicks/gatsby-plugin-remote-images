import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

const MainHeader = () => (
  <Header style={{ background: '#fff', padding: '0 16px' }}>
    <h1>gatsby-plugin-remote-images demo</h1>
  </Header>
);

export default MainHeader;
